/**
 * The window the summary trends measure, and the one they are compared against. Mirrors
 * `InstructorAnalyticsConstants.SummaryTrendWindowDays` on the server, which is what actually
 * decides the numbers — this value only names them in the UI.
 */
export const TREND_WINDOW_DAYS = 30;
