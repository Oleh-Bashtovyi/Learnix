export function AnnouncementBar() {
    return (
        <div className="bg-foreground px-4 py-2.5 text-center text-sm text-background">
            🎓 New: AI-assisted learning is now in every course.
            <a href="#features" className="ml-1 underline hover:text-primary">
                See how →
            </a>
        </div>
    );
}
