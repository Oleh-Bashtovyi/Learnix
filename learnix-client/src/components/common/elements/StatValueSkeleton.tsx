/** Placeholder for a `StatTile`'s `value` while it loads — a single pulsing bar, not a shaped skeleton. */
export function StatValueSkeleton() {
    return <span className="inline-block h-5 w-14 animate-pulse rounded bg-muted" />;
}
